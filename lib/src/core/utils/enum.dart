enum BlocStatus {
  initial,
  loading,
  success,
  error,
}

enum ProductStatus {
  active,
  deActive,
}

enum NetworkTypes {
  initial,
  connected,
  notConnected,
}

enum UserTypes {
  admin,
  user,
}

enum AuthTypes {
  signIn,
  signUp,
  forgot,
}

enum MarketingTypes {
  featured,
  trending,
  latest,
}

enum ResponseTypes {
  success,
  failure,
}

enum BackPageTypes {
  home,
  view,
}

enum CURDTypes {
  add,
  remove,
  update,
}

enum StoreTypes {
  coverImage,
  subImage,
  asset,
}
